# ===================================
# QUIZOCK ENVIRONMENT CONFIGURATION
# ===================================
# Copy this file to .env and update the values according to your setup
# This file contains configuration settings for the Quizock application

# ===================================
# SERVER CONFIGURATION
# ===================================

# PORT: The port number where your backend server will run
# Default: 5000 (you can change this if needed)
# Example: If you set PORT=3001, your backend will run at http://localhost:3001
PORT=5000

# ===================================
# DATABASE CONFIGURATION
# ===================================

# MONGODB_URI: Connection string for your MongoDB database
# This tells the application where to find your database
# Format: mongodb://[username:password@]host[:port]/database_name
# 
# For local MongoDB installation (most common):
MONGODB_URI=mongodb://localhost:27017/quizock
#
# If you're using MongoDB Atlas (cloud database), it will look like:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/quizock
#
# If you're using Docker with MongoDB:
# MONGODB_URI=mongodb://mongo:27017/quizock

# ===================================
# SECURITY CONFIGURATION
# ===================================

# JWT_SECRET: A secret key used to create secure login tokens
# IMPORTANT: Change this to a random, complex string in production
# You can generate one at: https://generate-secret.vercel.app/64
# Or use: openssl rand -base64 64
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_make_it_very_long_and_random

# SESSION_SECRET: Used for secure session management
# IMPORTANT: Change this to a random, complex string in production
# Should be different from JWT_SECRET
SESSION_SECRET=another_very_secure_random_string_for_sessions_change_this_too

# ADMIN_PASSWORD: Password to access the admin panel
# Change this to a strong password that only teachers/administrators know
# Students will NOT use this password - this is only for admin access
ADMIN_PASSWORD=SecureAdminPass123!

# ===================================
# OPTIONAL CONFIGURATION
# ===================================

# NODE_ENV: Application environment (development/production)
# Leave as 'development' for local setup, change to 'production' for live deployment
NODE_ENV=development

# FRONTEND_URL: URL where your React frontend is running
# This is used for CORS (Cross-Origin Resource Sharing) configuration
FRONTEND_URL=http://localhost:3000

# UPLOAD_LIMIT: Maximum file size for CSV uploads (in MB)
# Adjust based on your server capacity and needs
UPLOAD_LIMIT=10

# ===================================
# DATABASE BACKUP CONFIGURATION (OPTIONAL)
# ===================================

# BACKUP_SCHEDULE: Cron schedule for automatic database backups
# Format: minute hour day-of-month month day-of-week
# Example: "0 2 * * *" means daily at 2:00 AM
# BACKUP_SCHEDULE=0 2 * * *

# BACKUP_RETENTION_DAYS: How many days to keep backup files
# BACKUP_RETENTION_DAYS=30

# ===================================
# EMAIL CONFIGURATION (OPTIONAL - FOR FUTURE FEATURES)
# ===================================

# If you plan to add email notifications for student progress
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# ===================================
# SETUP INSTRUCTIONS
# ===================================
#
# 1. Copy this file to .env (remove .example from filename)
# 2. Install MongoDB on your computer OR use MongoDB Atlas (cloud)
# 3. Update MONGODB_URI with your database connection
# 4. Change JWT_SECRET and SESSION_SECRET to random, secure strings
# 5. Set a strong ADMIN_PASSWORD
# 6. Save the file and restart your application
#
# For detailed setup instructions, see TEACHER_GUIDE.md
#
